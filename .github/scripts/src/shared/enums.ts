export enum ChainDirectories {
  OSMOSIS = "osmosis",
  TERRA = "terra",
}

export enum JsonFiles {
  ACCOUNT = "account",
  ASSET = "asset",
  BINARY = "binary",
  CONTRACT = "contract",
  ENTITY = "entity",
  POOL = "pool",
}
